# Experiment Entry Template (extends hypothesis.md once DESIGNED)

```
DESIGN
  Unit of observation / cohort definition:
  Controls: (placebo / random gate / ungated baseline / benchmark)
  Declared grids (primary values marked):
  Folds / data slices (declared subset if any):
  Cost tier(s):
  Escalation plan (where M1 resolution is required):
  Statistician sign-off: (name/date; blocking level that gates)

DECISION RULE (verbatim, frozen before first confirmatory run)
  CONFIRM if ...
  WEAKEN  if ...
  REFUTE  if ...

DEV SCREEN (if applicable)
  Design + result. A failed screen closes the entry here.

PRE-REGISTRATION
  Git SHA of the frozen script committed before the run:

RESULTS
  Files:
  Headline (effect size + CI at gating blocking level):
  Sensitivity grid summary (worst corner):
  Dissection (by year / regime / exit reason / tails):
  Placebo/control comparison:

REVIEW PASSES
  Adversarial review (leakage / cost / selection) findings:
  Risk pass (tail mechanisms, §7.4) findings:

VERDICT
  State + one-paragraph justification against the decision rule.

LESSONS
  What this changes in program agendas; new PROPOSED entries spawned.
```
